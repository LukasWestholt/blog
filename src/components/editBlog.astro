{
	process.env.NODE_ENV === 'development' && (
		<a
			href={`https://github.com/LukasWestholt/blog`}
			target='_blank'
			class='fixed bottom-5 right-5 z-50 flex w-[120px] border-2 border-neutral-500 rounded-full items-center justify-center py-[2.5px] font-bold text-[12px] uppercase text-secondary bg-secondary-dark shadow-2xl'
		>
			Edit in GitHub
		</a>
	)
}
