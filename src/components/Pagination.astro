---
import ArrowLeft from './icons/ArrowLeft.astro'
import ArrowRight from './icons/ArrowRight.astro'

const { page } = Astro.props
---

<div class='flex gap-5 md:gap-1 justify-around md:justify-end'>
	<!-- Previous Button -->
	{
		page.start > 0 && (
			<a
				href={page.url.prev}
				class='flex items-center justify-center px-8 md:px-4 h-10 text-base font-medium text-secondary bg-secondary-dark border border-neutral-300 rounded-lg hover:bg-neutral-100 hover:text-neutral-700 dark:bg-transparent dark:border-neutral-700 dark:text-neutral-400 dark:hover:bg-neutral-700 dark:hover:text-secondary-dark'
			>
				<ArrowLeft />
			</a>
		)
	}

	<!-- Next Button -->

	{
		page.currentPage < page.lastPage && (
			<a
				href={page.url.next}
				class='flex items-center justify-center px-8 md:px-4 h-10 ml-3 text-base font-medium text-secondary bg-secondary-dark border border-neutral-300 rounded-lg hover:bg-neutral-100 hover:text-neutral-700 dark:bg-transparent dark:border-neutral-700 dark:text-neutral-400 dark:hover:bg-neutral-700 dark:hover:text-secondary-dark'
			>
				<ArrowRight />
			</a>
		)
	}
</div>
